---
- hosts: all
  user: "{{ server_user }}"
  become: yes
  become_method: sudo
  vars_files:
  - vars.yml
  roles:
    - nagios-install
    - defaults
    - nagios-plugins
    - configure-nagios
    - add-host